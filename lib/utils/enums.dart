enum CurrentScreen { home, communities, followed }

enum DefaultTab { hot, top, newTab, followed }

enum DefaultPeriod {
  sixHours,
  twelveHours,
  twentyFourHours,
  sevenDays,
  thirtyDays,
  all,
}
